import { FiMinus,FiPlus,FiTrash2 } from 'react-icons/fi'; import {useDispatch} from 'react-redux'; import {decreaseQuantity,increaseQuantity,removeFromCart} from '../redux/slices/cartSlice';
export default function CartItem({item}){const dispatch=useDispatch();return <div className="flex gap-4 border-b border-neutral-200 py-5"><img src={item.image} alt={item.name} className="h-28 w-24 rounded-xl object-cover sm:h-32 sm:w-28"/><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><div><h3 className="font-semibold">{item.name}</h3><p className="mt-1 text-xs text-neutral-500">Size: {item.size} · Color: {item.color}</p></div><p className="font-semibold">${(item.price*item.quantity).toFixed(2)}</p></div><div className="mt-6 flex items-center justify-between"><div className="flex items-center rounded-full border border-neutral-200"><button aria-label="Decrease quantity" onClick={()=>dispatch(decreaseQuantity(item.cartId))} className="p-2"><FiMinus size={13}/></button><span className="w-8 text-center text-sm">{item.quantity}</span><button aria-label="Increase quantity" onClick={()=>dispatch(increaseQuantity(item.cartId))} className="p-2"><FiPlus size={13}/></button></div><button onClick={()=>dispatch(removeFromCart(item.cartId))} className="flex items-center gap-1 text-xs text-neutral-500 hover:text-red-500"><FiTrash2/>Remove</button></div></div></div>}
